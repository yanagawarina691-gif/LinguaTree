/**
 * 内化容器页（P0 过渡方案）
 * 当前阶段先复用旧版 choice/fill/judge 三题型作为内化检测，
 * 完成后再进入迁移邀请弹窗。
 */
import TrainingPage from './TrainingPage.jsx';

export default function InternalizePage() {
  return <TrainingPage />;
}
